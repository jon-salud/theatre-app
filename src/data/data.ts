// src\data\data.ts

import { HospitalData } from '../interface/types';
import hospitalDataJson from './hospitalData.json';

// Export the data for use in other parts of your application
export const hospitalData: HospitalData = hospitalDataJson;